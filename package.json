{
  "name": "nvme-bin-viewer",
  "version": "2.0.2",
  "description": "Parse and analyze NVMe binary data including BAR0 registers and queue entries",
  "main": "dist/index.js",
  "bin": {
    "nvme-bin-viewer": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "build:web": "npm run build && npm run bundle:web",
    "bundle:web": "esbuild src/core.ts --bundle --format=iife --global-name=NVMeParser --outfile=web/nvme-parser.js",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/",
    "web/",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "nvme",
    "pcie",
    "bar0",
    "registers",
    "queue",
    "submission",
    "completion",
    "parser",
    "cli",
    "binary"
  ],
  "author": "wipeseals",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/wipeseals/nvme-bin-viewer.git"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
